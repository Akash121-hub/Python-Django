# STATIC ARRAY :- It has a fix size of elements

# DYNAMIC ARRAY:- It doesn't have fix size of array
    # When the size of the array grows then it is overhead of memory...memory given and elememnts copied (length of elements + (length of elements*2))